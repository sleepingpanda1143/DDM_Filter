# Python / tooling
__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/
*.egg-info/
.venv/
venv/

# Lightning / local training logs
lightning_logs/

# Regenerated at end of multi-preset runs
dataset/_timegrad_last_run_summary.json

# Heavy or reproducible artifacts under dataset presets (keep JSON/CSV preprocess in git if desired)
dataset/**/predictor/
dataset/**/timegrad_autoruns/
dataset/**/timegrad_runs*/
dataset/**/forecast_samples.npz
dataset/**/train_test_trajectories.npz

# Compare / sweep output trees (summaries are small; whole trees are reproducible)
dataset/compare_delta_gpu*/
dataset/exp_compare_gpu*/
dataset/exp_ablate_2gpu/
dataset/radar_compare_parallel_logs/
dataset/radar_compare_runs_gpu*/
